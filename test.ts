// basic JS data structure helpsheet
const shoppinngList = ["milk", "jerk chicken", "coffee", "new monitor"];

// this is const function - this will print list of strings seperated with comma

const user = {
  name: "dean",
  age: 25,
  isOver18: true,
  phoneNumber: "07894658273",
  email: "deannormington@gmail.com",
  isEmailVerified: true,
  isPhoneVerified: true,
};
// declaring an object (user) and using key/value pairs (name = key value = string)

const users = [];
users.push(user);
// this allows to add object (user above) to an empty array
// of users this creating list

const year = 2026;
const nextYear = year + 1;
// declaring const to find out next year we use year + 1
// this using math and declartion

/**
test knowledge after food:
1) difference between const vs let - let allows you to change data when declaring while const isnt
special word = const is an immutable variable (let can be mutated which means it is mutable)
2) function varible, object - 

  Function  = block of repetable code    
  Variable  = a reference to a value
  Object    = list of keys and values (user and info)
  Array     = list of data values (shopping list)
  Boolean   = the value of yes or no (true/false)
 

3) nesting and bracket type how to make sure?
parent families, making sure content is in its brackets
brackets can be put in an object 


*/
